#include<stdio.h>
int main()
{
    int i,n,p,c=0;
    scanf("%d%d",&n,&p);
    for(i=1;i<=p;i++)
    {
        c++;
        if(c<n)
            printf("%d ",i);
        else if(c==n)
        {
            printf("%d\n",i);
            c=0;
        }
    }
    return 0;
}
