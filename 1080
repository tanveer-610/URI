#include<stdio.h>
int main()
{
    int i,a[100],k,max;

    for(i=0;i<100;i++)
    {
        scanf("%d",&a[i]);
    }
    max=a[0];
    for(i=1;i<100;i++)
    {
        if(max<a[i])
        {
            max=a[i];
            k=i+1;
        }
    }
    printf("%d\n%d\n",max,k);
    return 0;
}
